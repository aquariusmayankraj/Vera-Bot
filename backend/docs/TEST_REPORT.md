# Current test report

This is version 2 of the full-stack package. The authoritative current report is [../../docs/TEST_REPORT.md](../../docs/TEST_REPORT.md); current evidence is in `../../reports/`.

The former ZIP's historical 97-test report was replaced rather than presented as a fresh run of this frontend. The backend suite now also covers CORS, HTML landing-page escaping, isolated demo routes, token handling and separate-database persistence.
